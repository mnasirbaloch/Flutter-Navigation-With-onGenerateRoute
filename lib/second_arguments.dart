// A Model class which will be used to send/recieve argument when navigation from one route to another
class SecondArguments {
  final String name;
  final String age;
  SecondArguments({required this.name, required this.age});
}
